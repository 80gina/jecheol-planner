# 제철밥상 시계열 분석 — 의존성
# 설치:  pip install -r requirements.txt
#
# 버전을 딱 고정하지 않고 "이 버전 이상"으로 적는다.
# 파이썬 버전이 새로우면 고정한 버전에 맞는 설치 파일이 없어서
# 컴퓨터에서 직접 빌드하려 들고, 그러면 Visual Studio 가 필요해진다.
# 범위로 적으면 pip 가 이 파이썬에 맞는 것을 알아서 고른다.

requests>=2.31            # KAMIS·기상청 API 호출
python-dotenv>=1.0        # .env 에서 인증키 읽기
pandas>=2.2               # 시계열 데이터 다루기
numpy>=1.26               # 수치 계산
matplotlib>=3.8           # 그래프 그리기

# 시계열 분해는 statsmodels 대신 직접 구현했다 (analyze.py 의 decompose).
# 이유: "어떻게 분해했나"를 설명할 수 있어야 하고, 의존성도 하나 줄어든다.
