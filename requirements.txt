# 제철밥상 시계열 분석 — 의존성
# 설치:  pip install -r requirements.txt

requests==2.32.3          # KAMIS API 호출
python-dotenv==1.0.1      # .env 에서 인증키 읽기
pandas==2.2.3             # 시계열 데이터 다루기
numpy==2.1.3              # 수치 계산
matplotlib==3.9.2         # 그래프 그리기
statsmodels==0.14.4       # 시계열 분해 (보너스)
